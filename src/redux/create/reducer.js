// const initialState = {};

// const CreateReducer = (state = initialState, action) => {
//   const {type, payload} = action;
//   switch (type) {
    
//     default:
//       return [...state];
//   }
// };

// export default CreateReducer;
